parameters:
  nginx:
    _support:
      collectd:
        enabled: true
      heka:
        enabled: true
      sensu:
        enabled: false
      sphinx:
        enabled: true
      grafana:
        enabled: true
      prometheus:
        enabled: true
      telegraf:
        enabled: true